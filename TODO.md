# TODO

- [x] Update `src/lib/supabase.ts` to remove demo/placeholder fallbacks for `VITE_SUPABASE_URL` and `VITE_SUPABASE_ANON_KEY`.
- [x] Add fail-fast error message when Supabase env vars are missing.

- [x] Keep `isSupabaseConfigured` as a useful boolean for gating UI logic.
- [ ] Restart dev server after env changes (user step) (after updating .env).



